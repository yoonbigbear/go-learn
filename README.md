## installation
- [] minikube
- [] helm

1. run `setup-infrastructure.bat`
2. run `dev_deploy.bat`
3. cmd `minikube tunnel`
4. cmd `cd game-lobby\client\`
5. cmd `go run main.go`

grafana - `http://localhost:3000` 
jaeder - `http://localhost:16686`
